#include<iostream>
#include"dir.hpp"
#include<fstream>
#include<ios>

using namespace std;
int nr;

int main()
{
    fstream plik;
    nr = menu();
    switch(nr)
    {
        num_line ++;
        cout<<num_line<<". "<<line;
    }
    inputFile.close();
    return 0;
}